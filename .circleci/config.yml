version: 2
jobs:
  test:
    macos:
      xcode: "10.2.0"
    steps:
      - checkout
      - run: bundle exec fastlane scan --scheme "f4s-f4s_staging"
    macos:
      xcode: "10.2.0"
  deploy:
    macos:
      xcode: "10.2.0"
    steps:
      - checkout
      - deploy:
        name: Deploy
        command: fastlane release_appstore

workflows:
  version: 2
  test:
    jobs:
      - test
#      - deploy:
#        requires:
#          test
#        filters:
#        branches:
#          only: master
